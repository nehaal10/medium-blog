import {type useResPayload} from './model/model'
import { type createUserRequestType } from '@mediumblog/common'
export let createUser = async (createUseReq: createUserRequestType): Promise<useResPayload> => {
    // here it self i will call the db but good practise have all sb clals in one folder
    
    let x : useResPayload = {}
    return x
}   